const initialState = {
 
};

export default function commonReducer(state = initialState, action) {
  switch (action.type) {
    case 'PROJECT_INIT':
      return state
    default:
      return state
  }
}